# Pausa segura · 2026-09-15 (~15:25, hora de Hermosillo)

**RETOMADA el 2026-09-15 a las ~19:50, sin rehacer nada, y CERRADA con el acta
`docs/actas/2026-09-15-fase-4a.md`.** La Fase 4a llegó hasta el final: commit
`2a0aba3e01dcd15878579f1d02a64457b1834046`, desplegado en la Pi y probado en
vivo sin papel a las 21:32. **Todo lo que sigue es el estado del momento de la
pausa y se conserva tal cual, como evidencia de cómo se retomó.**

Escrito por el orquestador (Fable) a petición del usuario. Fuente durable del estado
exacto y de cómo retomar. Las pausas anteriores (`docs/PAUSA-2026-09-11.md` y
`docs/PAUSA-2026-09-11-noche.md`) ya se retomaron y cerraron.

## 1. Estado al pausar (medido por un agente verificador de solo lectura)

| Qué | Estado |
|---|---|
| Repo publicado | **13 commits** en `main`; `HEAD` = `origin/main` = `3290930c279795251db392491eaf5a3033bfbaf1` (plan de la Fase 4a). Antes: `a554be2` (cierre de Fase 3) y `c7b5aa0` (documento del evento) |
| Árbol de trabajo | **10 archivos modificados sin commitear**, nada en el índice (ver §3): es el trabajo **a medias** del ejecutor de código de la Fase 4a. La suite da **214 pruebas OK** con esos cambios (eran 194 antes) |
| Cadena en curso | **detenida** con TaskStop: workflow `ruleta-fase4a-papel`, run `wf_b070d43e-66f`, task `wmnuviim5`, script `workflows/scripts/ruleta-fase4a-papel.js` (en la carpeta de la sesión, ver §5) |
| Raspberry Pi 5 (`ruleta`, `asadero`) | Servicio `ruleta` **active** y **enabled**, `NRestarts=0`. Clon en **`5345d25`** (3 commits de docs por detrás; **el código de la Fase 4a NO está desplegado**). Árbol de la Pi limpio salvo `config.json.bak-2026-09-12` (sin seguimiento). Inventario real en **folio 10** (boletos 1–10 de pruebas, todos impresos). En `/tmp`: `sonda-papel.py/.log/.out` (se borran al reiniciar). Botones JUGAR y HABILITAR cableados y probados; sin LED; sin batería RTC |
| Red | Pi en la Wi-Fi "Miltimex 5G", IP 192.168.50.168; alias ssh `ruleta` funciona. En el asadero tendrá el internet del negocio (decisión del usuario del 2026-09-15) |
| Comportamiento del kiosco HOY | El de `5345d25`: **no detecta papel agotado** (medido a las 13:29: emite, descuenta y da por impreso un boleto que no sale; la impresora lo retiene y lo imprime al reponer papel). **Regla temporal: no dejar que se acabe el rollo.** El aviso falso de "poco papel" sigue saliendo en cada arranque; ignorarlo |

## 2. Dónde se quedó el agente, exactamente

Cadena de la Fase 4a (10 eslabones). Estado por eslabón:

1. `ejecutor:plan-f4a` ✅ escribió `docs/planes/fase-4a-papel.md` (1180 líneas), la copia de hechos `docs/actas/2026-09-15-hechos-medidos-fase-4a.md` y una ficha de apertura.
2. `lente:plan-f4a` ✅ devolvió 6 correcciones; `correctivo:plan` ✅ las aplicó todas.
3. `commit:plan` ✅ commit `3290930` (base `a554be2`), push observado; `verificar:plan` ✅ HEAD = origin/main, árbol limpio, 13 commits.
4. `ejecutor:codigo-f4a` ⏹ **DETENIDO A MITAD** (corrió de 14:57 a 15:25). Lo que ya está en el árbol (resumen del verificador, sin juzgar):
   - `ruleta/escpos.py`: nueva `leer_estado_fresco()` (drena rezagados con tope y se queda con el **último** byte válido), usada por `ImpresoraBluetooth._leer_estado` y `ImpresoraArchivo._leer_estado` (se retiró `_MAX_BYTES_BASURA`); constantes nuevas `CMD_ESTADO_CAUSA` (DLE EOT 2), `BIT_TAPA_ABIERTA`, `BIT_FIN_DE_PAPEL`, `BIT_ERROR_IMPRESORA` y los topes `_MAX_BYTES_DRENADO`, `_ESPERA_DRENADO_SEG`, `_MAX_BYTES_RESPUESTA`, `_ESPERA_SIGUIENTE_BYTE_SEG`, `_MAX_SEG_RESPUESTA`; `verificar_estado()` consulta papel → causa → estado; máscara estricta de poco papel; `consultar_papel()` devuelve `(papel, causa, estado)`.
   - `ruleta/__main__.py`: `interpretar_estado_papel(papel, causa, ...)` con los bits de causa.
   - `tests/test_escpos.py` (+418 líneas, ~19 pruebas nuevas), `tests/test_app.py` (+6), `tests/test_instalacion.py` (+45). Suite: 214 OK.
   - Docs: `README.md` (+32), `docs/fichas.md` (+152), `docs/planes/fase-2-impresora.md` (6), `docs/actas/2026-09-11-fase-2.md` (+10, nota fechada), `docs/planes/fase-4a-papel.md` (74, bitácora).
   - **No consta** (el agente no llegó a informar): si corrió la tabla de mutaciones M1–M10 sobre una copia, si aplicó todas las correcciones C1–C8 del escéptico del 13, si la bitácora del plan quedó marcada hasta donde llegó, ni si `ruleta/app.py` necesitaba cambios (no fue tocado).
5. `lente:conducta`, `lente:fidelidad`, correctivos, `esceptico:f4a`, `commit:codigo`, `verificar:codigo`, `deploy:f4a`, `vivo:f4a`: **no iniciados**. Nada del código está publicado ni desplegado.

## 3. Archivos sin commitear (`git diff --stat`)

```
 README.md                       |  32 ++-
 docs/actas/2026-09-11-fase-2.md |  10 +
 docs/fichas.md                  | 152 ++++++++++++++-
 docs/planes/fase-2-impresora.md |   6 +-
 docs/planes/fase-4a-papel.md    |  74 +++++--
 ruleta/__main__.py              |  27 ++-
 ruleta/escpos.py                | 185 +++++++++++++-----
 tests/test_app.py               |   6 +
 tests/test_escpos.py            | 418 +++++++++++++++++++++++++++++++++++-----
 tests/test_instalacion.py       |  45 ++++-
 10 files changed, 823 insertions(+), 132 deletions(-)
```
Más este archivo (`docs/PAUSA-2026-09-15.md`), nuevo, sin seguimiento.

## 4. Qué se hizo hoy (2026-09-15) y qué falta

**Hecho y publicado**
- Fase 3 (botones) ejecutada en vivo y cerrada en papel: diagrama de cableado, plan retroactivo, acta, hechos, fichas F-239..F-250, README (commit `a554be2`). Cableado final: JUGAR NO→pin 11 (GPIO 17) y COM→pin 9 (GND); HABILITAR señal→pin 13 (GPIO 27) y negro→pin 25 (GND). Tres jugadas reales impresas (folios 1–3) y más pruebas del usuario hasta el folio 10.
- Prueba sin papel medida (13:29) y sonda de estado con lectura fresca (13:40–13:45): sin papel y tapa cerrada la impresora contesta `DLE EOT 2 = 0x32` (bit 5, fin de papel); `DLE EOT 4` siempre `0x12` y `DLE EOT 1` siempre `0x16`; tapa abierta no se reporta; `GS r 1` inútil. Hechos en `docs/actas/2026-09-15-hechos-medidos-fase-4a.md`.
- Plan prescriptivo de la Fase 4a (commit `3290930`), con las decisiones D1–D8 cerradas.

**Pendiente, en orden**
1. Terminar la Fase 4a: retomar la cadena (§5). Luego la **prueba en vivo con el usuario**: jugada sin papel → el journal debe decir que la impresora no tiene papel, el boleto se revierte y el premio vuelve; después una jugada con papel normal. Luego escriba + acta de Fase 4a por la cadena.
2. `CLAUDE.md` «Contexto del producto» (ficha F-242): ya no es «producción sin red» ni «batería RTC necesaria»; lo actualiza el orquestador dentro de un commit de docs (fuera del conjunto permitido de la cadena de código).
3. Decisiones del usuario: soldar o enrollar los cables del pulsador HABILITAR (F-239); `"led": null` o dejarlo (F-240); repositorio público o privado; hoja de instrucciones para el personal.
4. Fase 4 (código): pieza A consuelo con probabilidad, B franjas horarias por premio, C horario del evento, D esperar la sincronización de hora al arrancar (F-241). Antes hace falta que el usuario conteste las preguntas abiertas de `docs/evento-2026-09-asadero-33.md` (jugadas por día, franjas, nombres).
5. Antes del lunes 21: premios reales en `config.json`, `python3 -m ruleta reiniciar --si` (folio a cero), `git pull` en la Pi, prueba general. Borrar en la Pi `~/ruleta/config.json.bak-2026-09-12` y `~/config.json.pi-antes-deploy` cuando el usuario lo autorice.

## 5. Cómo retomar (orden exacto)

1. Leer: este archivo, `CLAUDE.md`, `docs/planes/fase-4a-papel.md` (bitácora y decisiones D1–D8), `docs/actas/2026-09-15-hechos-medidos-fase-4a.md`, `docs/fichas.md` (F-239..F-250 y la de apertura de la Fase 4a).
2. Verificador de solo lectura (Sonnet): `git status --porcelain -uall` muestra exactamente los 10 archivos del §3 más este; `HEAD` = `origin/main` = `3290930`; la suite da 214 OK; la Pi responde por `ssh ruleta` y sigue en `5345d25` con el servicio active.
3. **Editar el script** `C:/Users/seduv/.claude/projects/C--Users-seduv-Desktop-Ruleta-Asadero/5bf7e7ff-82b1-41cd-80f4-42ffcfb9b5c2/workflows/scripts/ruleta-fase4a-papel.js` antes de relanzar:
   - añadir `'docs/PAUSA-2026-09-15.md'` a `PERMITIDOS_CODIGO` (si no, el gate del agente de commit se detiene por una ruta fuera del conjunto);
   - **amendar el prompt del ejecutor de código** (`ejecutor:codigo-f4a`) con un párrafo de ESTADO PREVIO: «el árbol ya contiene tu trabajo a medias (los 10 archivos del §3 de `docs/PAUSA-2026-09-15.md`, suite 214 OK); NO rehagas: lee la bitácora del plan y el diff, comprueba lo que falta (mutaciones M1–M10 sobre una copia, correcciones C1–C8, `app.py` si el plan lo exige, bitácora marcada) y continúa desde ahí». Regla del protocolo §6: diagnóstico primero y nunca rehacer.
4. Relanzar `Workflow` con `scriptPath` = ese archivo y `resumeFromRunId = wf_b070d43e-66f`: plan, lente del plan, commit del plan y verificador vuelven de caché; el ejecutor de código corre en vivo con el prompt amendado; el resto de la cadena sigue igual (lentes → correctivo → escéptico → commit → deploy → verificación en vivo).
5. Al terminar la cadena, pedir al usuario la prueba sin papel (§4, punto 1) con el servicio activo y el código nuevo desplegado, y después lanzar la cadena de escriba + acta de la Fase 4a (incluir `CLAUDE.md` «Contexto del producto» en su conjunto permitido).

Regla del protocolo: si el código real contradice el plan, el ejecutor se detiene y
pregunta; no improvisa.
