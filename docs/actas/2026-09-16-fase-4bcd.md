# Acta · Fases 4b, 4c y 4d · Los premios reales, el consuelo con peso y el reparto por horas

- **Fecha:** del **2026-09-15 por la noche** al **2026-09-16 por la tarde**.
  **Todas las horas de este acta son las del reloj de la Pi**
  (`America/Hermosillo`), salvo donde se diga otra cosa.
- **Fases:** **4b** (premios reales en `config.json`), **4c** (pieza A: el
  consuelo con peso propio) y **4d** (piezas B, C y D: franjas, horario del
  evento, espera de la hora, y encima de todo eso el **reparto del cupo diario
  por horas**). Planes prescriptivos:
  `docs/planes/fase-4b-config-oficial.md`, `docs/planes/fase-4c-consuelo-peso.md`
  y `docs/planes/fase-4d-horas.md`, los tres escritos **antes** de tocar el
  archivo que cambian.
- **Commits de código:**
  - **`f810bc4d039b97127670b46f9aa6b8371a8db22a`** (Fase 4b), sobre `9afdd9c`.
    **16** commits en `main`.
  - **`d01a0ca61a531f90b70a20fb6042db8e295a6d38`** (Fase 4c), sobre `f810bc4`.
    **17** commits.
  - **`613f87552afefe5daaf5c1aa20bf5bbf1e811af3`** (Fase 4d), sobre `d01a0ca`.
    **18** commits.
  - Los tres con **push verificado por un agente distinto del que commiteó**:
    `HEAD` = `origin/main` y árbol limpio después de cada uno.
- **Resultado:** **el kiosco ya reparte los premios reales del evento a lo largo
  de las horas, y se probó en vivo.** A eso de las **13:00** del 2026-09-16, con
  `613f875` desplegado y el inventario recién puesto en cero, el usuario jugó y
  dijo: **«ya probé, salieron consuelos y una cerveza, funciona bien»**. **LAS
  TRES FASES QUEDAN CERRADAS** el 2026-09-16. Lo que falta para abrir el lunes 21
  **no es programación**: son las fechas `desde`/`hasta` y el reinicio del
  inventario (§10).
- **Fuente de este acta:** el **archivo de hechos medidos** de la sesión, copiado
  íntegro y literal en `docs/actas/2026-09-16-hechos-medidos-fase-4bcd.md`, y el
  **diff real** de los tres commits, leído con `git show` por un escriba que
  **no vio trabajar al ejecutor**. **Nada de este documento viene de memoria.**
  Lo que no se midió, se dice que no se midió. Donde el archivo de hechos y el
  diff dicen cosas distintas, **manda el diff** y la diferencia se anota (§11.1).

> **Sin secretos.** Aquí no hay contraseñas —ni la del Wi-Fi, ni la del punto de
> acceso, ni la del usuario `asadero`—, ni la llave privada `~/.ssh/id_ruleta`,
> ni ningún dato de cliente.

---

## 1. Participantes

| Quién | Rol en estas tres fases |
|---|---|
| El usuario (dueño de Asadero 33) | Las tres decisiones que las disparan, y **jugó en vivo** las tres veces. Suyo es el reporte que abre la Fase 4c y la confirmación que cierra la 4d |
| **Fable** (orquestador) | Midió los hechos antes de cada fase, escribió los tres briefs, armó las tres cadenas y decidió los gates. Aceptó por escrito las tres lecturas del ejecutor de la §7 |
| Ejecutor de código (Opus) | Una pasada por fase: plan, `config.json`, `ruleta/`, goldens, mutaciones y documentos **en el mismo cambio**. **No commiteó nunca** |
| Lentes y escéptico (Opus, solo lectura) | 4b: **9** + **2** correcciones, escéptico **2**. 4c: **6** + **5**, **3 sin converger** aplicadas por orden del orquestador, escéptico **1**. 4d: **7** + **3**, **2 sin converger** aplicadas por orden del orquestador, escéptico **2** |
| Agentes de commit (Opus) | Los únicos que tocaron git. Tres commits y tres pushes |
| Verificadores de push y en vivo (Sonnet, solo lectura) | Uno por commit contra el remoto; otro por deploy contra la Pi |
| Agentes de deploy (Opus) | Los tres `git pull --ff-only`, la suite en la Pi, los `restart` y el reinicio del inventario de las 12:46 |
| Extractor y escriba (Sonnet / Opus, solo lectura) | Inventario crudo del diff y este cierre documental |

---

## 2. Contexto y decisiones del usuario

### 2.1 La noche del 2026-09-15: «cambiar la Pi al programa oficial»

Petición literal del usuario, al cerrar la Fase 4a:

> «cambiar la Pi al programa oficial que usaremos; haré pruebas mañana para hacer
> algunos cambios y que quede todo listo para el lunes».

De ahí nacen las tres fases: la Pi deja los premios de relleno `TEST 1`…`TEST 7`,
el usuario prueba **en papel** el 16 y corrige lo que no le guste **antes** del
lunes 21.

**Punto de partida medido:** `main` limpio sobre `9afdd9c`, suite en **214 OK**
en la PC, y en la Pi el código de la Fase 4a (`2a0aba3`) con el inventario de
pruebas en **folio 16**. La Pi con internet —el Wi-Fi `SL-Durazo` de la casa del
usuario durante estos días, `192.168.1.212`, `NTP yes`—, impresora USB, botones
cableados, **sin LED** y **sin batería RTC**.

### 2.2 La mañana del 2026-09-16: «solo han salido premios»

Probando el kiosco ya con los premios reales, el usuario reportó, literal:

> «no ha salido ningún boleto de gracias por participar, solo han salido premios».

No era una avería: era **exactamente** lo que el §5.2 del documento del evento
tenía anunciado desde el **2026-09-13** y la ficha **F-261** desde el
**2026-09-15**. Sin la **pieza A**, el
consuelo solo salía cuando **ningún** premio estaba disponible, así que **las
primeras 34 jugadas del día ganaban, una tras otra**. Esa frase del usuario es la
que abre la Fase 4c.

### 2.3 El mediodía del 2026-09-16: «es imposible saber cuántas jugadas habrá»

El modelo entero del documento del evento colgaba de **N**, el número de jugadas
esperadas en un día. El usuario dijo que ese número **no se puede saber**, y con
eso tiró el §2 viejo. Sus decisiones de ese momento, confirmadas con un
**«así va, apúntalo y lanza la cadena»**:

- **N es imposible de saber** → los premios se reparten **por horas**.
- Evento de **12:00 a 23:00**.
- Los **siete premios confirmados**, con dos descripciones cambiadas por él:
  **CERVEZA** → «Tecate Light, Tecate Roja o Indio» y **AGUA FRESCA** →
  «Horchata, Jamaica o Cebada». Los tacos **son dos premios**, no uno.
- **Hielera** solo de **19:00 a 23:00**, jueves 24 y viernes 25.
- **Silla** y **set BBQ**: una pieza de **13:00 a 16:00** y otra de
  **19:00 a 22:00**.
- **Tacos, cerveza y agua**, parejo en las once horas.
- Una pieza liberada **no la gana forzosamente la primera jugada** que llegue.
- Las piezas **no se acumulan por adelantado**.
- **Fuera de horario → consuelo**; «todos los demás deben ser de gracias por
  participar».

---

## 3. Fase 4b · Los siete premios reales en `config.json`

### 3.1 Lo que cambió, leído del diff real de `f810bc4`

**Seis archivos, `722+` / `25−`.** Lo primero que dice el diff:
**`f810bc4` NO toca ni una línea de `ruleta/`**, que es justo lo que prohibía el
§6 de su plan.

#### `config.json` (+7 / −7)

**Solo la lista `premios`.** Ninguna otra llave aparece en el diff. Los siete de
relleno se van y entran los siete del §5.1 del documento del evento, **sin
`desde`/`hasta`** (decisión **D1**):

| `id` | `nombre` | `detalle` | `stock` | `tope_diario` | `peso` |
|---|---|---|---|---|---|
| `hielera` | HIELERA IGLOO | Premio mayor | 2 | 1 | 1 |
| `silla` | SILLA DE PLAYA | Premio grande | 10 | 2 | 2 |
| `bbq` | SET BBQ | Premio grande | 10 | 2 | 2 |
| `tacos3` | 3 TACOS DE PASTOR | Plato de 3 tacos de pastor | 20 | 4 | 4 |
| `tacos2` | 2 TACOS DE PASTOR | Plato de 2 tacos de pastor | 20 | 4 | 4 |
| `cerveza` | CERVEZA | Una cerveza | 50 | 10 | 10 |
| `agua` | AGUA FRESCA | Vaso de 1/2 litro | 55 | 11 | 11 |

**Suma de `stock` = 167. Suma de `tope_diario` = 34.**

#### `tests/test_config.py` (+110 / −3)

- **Constantes nuevas:** `RAIZ`, `RUTA_CONFIG`, `RUTA_EVENTO` y
  `CLAVES_DEL_DOCUMENTO = ("id", "nombre", "detalle", "stock", "tope_diario", "peso")`,
  con un comentario que dice **por qué no están `desde` ni `hasta`** y a qué
  golden se agarra eso.
- **Clase nueva `TestPremiosOficialesDelEvento`.** Su ayudante
  `premios_del_documento()` **parsea el documento**: busca todos los bloques
  ` ```json ` del archivo, exige que **exactamente uno** traiga `"premios"`, lo
  envuelve en llaves para poder leerlo tal cual está escrito y comprueba que
  trae **siete** premios con las seis llaves. Los dos goldens:
  - `test_config_json_lleva_exactamente_los_premios_del_documento`: compara
    **campo por campo y en orden**, lista contra lista.
  - `test_los_premios_del_config_todavia_no_traen_fechas`: comprueba que
    `config.json` **no** trae `desde` ni `hasta`, **y que el documento sí las
    trae**, para que la F-259 no se quede sin fuente de dónde copiarlas. Su
    docstring dice, con todas sus letras, que **está escrita para caerse** el día
    en que se carguen las fechas, y qué hay que hacer entonces: añadirlas a
    `CLAVES_DEL_DOCUMENTO`, **no** relajar el assert.
- **`test_config_json_del_proyecto_es_valido` deja de anclar relleno.** Donde
  decía `len(cfg.premios) == 7` y `cfg.premios[2].stock == 1` (que era el
  `TEST 3`), ahora ancla **censos derivados**: los siete ids **en su orden**, el
  `stock` de la hielera y el del agua, y **las dos sumas**, 167 y 34.

### 3.2 Goldens y mutaciones

**Suite de 214 a 216** pruebas en verde. **Siete mutaciones, las siete en rojo**,
cada una sobre una copia limpia del repositorio:

| # | Qué se mutó | Resultado |
|---|---|---|
| M1 | `config.json`: `stock` del agua 55 → 54 | **ROJO** (2 fallos) |
| M2 | `config.json`: `CERVEZA` → `CHELA` | **ROJO** |
| M3 | `config.json`: quitar el premio `bbq` | **ROJO** (2 fallos) |
| M4 | `config.json`: `peso` del agua 11 → 12 | **ROJO** |
| M5 | `config.json`: `detalle` «Una cerveza» → «Una chela» | **ROJO** |
| **M6** | **el DOCUMENTO**: `stock` del agua en el §5.1, 55 → 54 | **ROJO** |
| **M7** | **el DOCUMENTO**: romper la valla del bloque (` ```json ` → ` ```jsonc `) | **ROJO** (2 fallos) |

**M6 y M7 son las que importan:** mutan **el documento** con `config.json`
intacto. Son la prueba de que el golden **deriva** del documento en vez de
comparar contra una copia escrita a mano dentro del test.

### 3.3 Revisión, commit y fichas

- **Lentes:** ronda 1, **9** correcciones; ronda 2, **2**. **Escéptico: 2**, las
  dos por sobreafirmación —el documento del evento y el `README.md` decían que el
  `config.json` y el documento «no se pueden desincronizar», y eso solo es cierto
  para el bloque del §5.1, no para la tabla del §1—.
- **Fichas: 258 → 262.** **F-259** (cargar `desde`/`hasta` antes del lunes),
  **F-260** (nombres y detalles con asterisco, sin confirmar), **F-261** (pieza A:
  las primeras 34 jugadas del día ganan seguro) y **F-262** (volver a reiniciar
  el inventario el lunes si el 16 se juega).
- **Commit `f810bc4`**, con los seis archivos que el Paso 8 del plan fijó por
  adelantado. Push verificado contra el remoto por un agente distinto.

### 3.4 Deploy y verificación en vivo (00:10–00:11 del 2026-09-16)

`git pull --ff-only` de `2a0aba3` a **`f810bc4`**. El agente de deploy anotó
**17 archivos**, y así quedó en el archivo de hechos; el **diff real** entre esos
dos commits toca **12** (`git diff --name-only 2a0aba3 f810bc4`). **Manda el
diff**, y la diferencia se anota aquí porque el número del archivo de hechos **no
se pudo reproducir** desde el repositorio (misma regla que la §11.1). Medido
allá:

- **216 pruebas OK en la Pi**, el mismo número que en la PC.
- Servicio detenido y **`python3 -m ruleta reiniciar --si`**, con esta salida
  literal:

  > `Folio actual: 00016. Premios entregados registrados: 0.`
  > `Respaldo: datos/estado_20260916_001053.json / datos/boletos_20260916_001053.csv.`
  > `Inventario reiniciado. Folio en 00000.`

  **Ojo con lo que respalda `reiniciar`:** `estado.json` y `boletos.csv`, **no
  `ruleta.log`**; y `boletos.csv` **se mueve** y se vuelve a crear con el primer
  boleto (§9.2).
- `start` a las **00:11:02** → `active`, **`NRestarts=0`**.
- Journal de arranque:
  `Ruleta arrancando. Premios: hielera, silla, bbq, tacos3, tacos2, cerveza, agua`
  e `Inventario impreso (arranque). Folio actual 00000`.
- **Cero avisos de poco papel.** Verificado en vivo por un agente distinto del
  que desplegó.

---

## 4. Fase 4c · Pieza A: el consuelo entra al sorteo con peso propio

### 4.1 Lo que cambió, leído del diff real de `d01a0ca`

**Quince archivos, `1 120+` / `73−`.** Cinco de código, cinco de pruebas, cinco
de documentos y configuración.

#### `ruleta/config.py` (+10 / −0)

- `ConfigConsuelo` gana **`peso: int = 0`**, con el comentario que explica que
  son «papelitos del boleto de consuelo en la tómbola» y que **0 = como antes de
  la Fase 4c**.
- `validar()` gana el rechazo del negativo, con un mensaje que **nombra la
  llave**: «`juego.consuelo.peso` no puede ser negativo (se encontró …): son los
  papelitos del boleto de consuelo en la tómbola. 0 = el consuelo solo sale
  cuando ya no queda ningún premio disponible».

#### `ruleta/inventario.py` (+41 / −7)

- **La cabecera del módulo se reescribe.** La frase que citaba la ficha F-261
  —«Si no hay ninguno disponible, el sorteo devuelve None»— deja de ser la regla
  y pasa a ser **un caso**; se añaden las tres reglas del consuelo con peso.
- `Inventario.__init__` gana **`peso_consuelo: int = 0`**.
- `Resumen` gana **`peso_consuelo`** y **`probabilidad_consuelo`**.
- `probabilidades()` mete el consuelo **en el denominador**, y su docstring avisa
  de la consecuencia: **con peso > 0 los porcentajes de los premios ya no suman
  100**.
- **Función nueva `probabilidad_consuelo(momento)`**, con el caso borde escrito:
  sin premios disponibles devuelve **100.0**.
- **`sortear()` reescrito: UNA sola elección ponderada.** La población es
  `disponibles + [None]` y los pesos, los de los premios más `peso_consuelo`. El
  `None` **solo** se añade si `peso_consuelo > 0`. Sin premios disponibles
  devuelve `None` **sin tirar**.

#### `ruleta/__main__.py` (+3 / −2)

- `abrir_inventario()` pasa **`peso_consuelo=cfg.juego.consuelo.peso`**.
- En `vista-previa`, el rótulo **«=== Boleto de consuelo (sin premios
  disponibles) ===»** pasa a **«=== Boleto de consuelo (la jugada no dio premio)
  ===»**, porque el viejo dejó de ser cierto.

#### `ruleta/app.py` (+8 / −1)

El único `log.warning("Sin premios disponibles: boleto de consuelo …")` **se
parte en dos**: si quedan disponibles, `log.info` **«Boleto %s: el sorteo cayó en
el consuelo (peso %d)»**; si de verdad no queda ninguno, el **warning de
siempre**, palabra por palabra.

#### `ruleta/ticket.py` (+6 / −0)

`boleto_inventario` añade el renglón del consuelo **solo si
`resumen.peso_consuelo > 0`**, armado con `_dos_columnas` para que nunca pase del
ancho.

#### `config.json` (+2 / −1)

`juego.consuelo` gana **`"peso": 217`**, que es la regla **N − 33** del documento
con **N = 250**, la propuesta por omisión de la pregunta 1 del §4.

### 4.2 Goldens y mutaciones

**Suite de 216 a 232** pruebas en verde, **+16**. Lo nuevo, por archivo:

- **`tests/test_inventario.py`:** doble nuevo **`RngEspia`**, que **captura la
  llamada a `choices`** y permite anclar **por igualdad** la población y los
  pesos. Seis goldens:
  `test_sortear_con_peso_de_consuelo_mete_none_en_la_tombola`,
  `test_sin_peso_de_consuelo_la_tombola_es_solo_de_premios`,
  `test_sin_premios_disponibles_devuelve_consuelo_sin_sortear`,
  `test_el_consuelo_entra_en_el_denominador_de_las_probabilidades`,
  `test_el_consuelo_no_toca_stock_ni_tope_pero_gasta_folio` y
  `test_distribucion_aproximada_con_peso_de_consuelo`.
- **`tests/test_config.py`:** `test_peso_de_consuelo_con_tipos_malos`,
  `test_consuelo_sin_peso_vale_cero`,
  `test_config_json_lleva_el_peso_de_consuelo_del_documento` (derivado del
  **§5.2 A** del documento) y
  `test_el_peso_del_documento_obedece_la_regla_N_menos_33_del_su_propio_2`, que
  **obliga al documento a ser consistente consigo mismo**: el peso del §5.2 tiene
  que cuadrar con la tabla del §2.
- **`tests/test_instalacion.py`:** clase nueva **`TestInventarioDeProduccion`**,
  con `test_abrir_inventario_pasa_el_peso_del_consuelo` (dos `cfg` distintos, dos
  inventarios distintos: **el peso se deriva**) y
  `test_solo_hay_un_sitio_de_produccion_que_construye_el_inventario`.
- **`tests/test_ticket.py`:** `test_inventario_con_peso_de_consuelo` (la línea
  entera **por igualdad**, a 48 y a 32 columnas),
  `test_inventario_del_config_real_cabe_entero` (el vector real: la línea ocupa
  **las 48 columnas justas**, anclado con `len(fila) == ancho`) y
  `test_inventario_sin_peso_de_consuelo_no_menciona_el_consuelo`.
- **`tests/test_app.py`:** doble **`RngSiempreConsuelo`** y
  `test_el_consuelo_con_peso_sale_aunque_queden_premios`.

**Doce mutaciones, las doce en rojo**, cada una con el test que la tabla del §5
del plan predecía. La **M8** —`"peso": 217` → `267` **en el documento**, con
`config.json` intacto— es la que demuestra que el golden **deriva**.

### 4.3 Lo que midió el programa

- Tómbola con el `config.json` real: **34 papelitos de premio + 217 del consuelo
  = 251**; **gana el 13.55 %** de las jugadas, 1 de cada 7 u 8, en vez del 100 %
  de las primeras 34.
- Simulación: con peso 217, **250 jugadas reparten 32 premios**, en vez de 34
  seguidos al empezar el día.

### 4.4 Revisión, commit y fichas

- **Lentes:** **6** + **5** correcciones, con **3 sin converger** que se
  aplicaron **por orden del orquestador** (§9.5). **Escéptico: 1.**
- **Fichas: 262 → 266.** **F-263** (la N sigue siendo propuesta mía, no decisión
  del usuario), **F-264** (`juego.consuelo.peso` acepta cualquier entero sin
  aviso), **F-265** (quien construye el `Inventario` es `__main__.py`, no
  `app.py`, contra lo que decía el brief) y **F-266** (el §5.1 del documento
  seguía diciendo que la Pi va sin red). **F-261 quedó cerrada** en este mismo
  commit.
- **Commit `d01a0ca`**, con los quince archivos del §7 del plan. Push verificado.

### 4.5 Deploy y verificación en vivo (10:47:25)

`git pull --ff-only` → **`d01a0ca`**; **232 pruebas OK en la Pi**; `restart`;
`active`, **`NRestarts=0`**; journal con **«Inventario impreso (arranque). Folio
actual 00005»** y **0 ERROR / 0 WARNING**. Verificado en vivo por un agente
distinto.

El **folio 00005** no es un error: entre el reinicio de las 00:11 y este deploy,
**el usuario jugó cinco veces** esa mañana —**agua 3, bbq 1, cerveza 1**—, que es
justo lo que la Fase 4c venía a arreglar.

**El agente de deploy señaló, de paso, que `CLAUDE.md` y la ficha F-243 seguían
diciendo «folio 16»**, y que eso ya no era cierto. Ese aviso es una de las
razones de la fila 6 de la §10 de este cierre y del párrafo del inventario que
se reescribió en `CLAUDE.md`.

---

## 5. Fase 4d · Piezas B, C y D, y el reparto por horas

### 5.1 Lo que cambió, leído del diff real de `613f875`

**Quince archivos, `2 415+` / `575−`.** `ruleta/hardware.py` estaba **permitido**
en la compuerta del commit y **no hizo falta tocarlo** (ficha **F-272**).

#### `config.json` (+20 / −8)

- `juego` gana **`separacion_min_entre_premios: 3`**, **`espera_hora_seg: 120`**
  y el bloque **`horario`**: `{ "abre": "12:00", "cierra": "23:00",
  "fuera_de_horario": "consuelo" }`.
- **`juego.consuelo.peso` baja de 217 a 10.** Con el reparto por horas el
  consuelo ya no compite contra los 34 papelitos del día entero, sino contra las
  **una o dos piezas abiertas** en ese momento.
- **Franjas:** `hielera` `[19:00–23:00, tope 1]`; `silla` y `bbq`
  `[13:00–16:00, tope 1]` y `[19:00–22:00, tope 1]`.
- **Dos descripciones nuevas, dictadas por el usuario:** `cerveza` →
  «Tecate Light, Tecate Roja o Indio»; `agua` → «Horchata, Jamaica o Cebada».

#### `ruleta/config.py` (+120 / −1)

- Constantes nuevas `_HORA_RE` y `FUERA_DE_HORARIO = ("consuelo", "no_jugar")`,
  y función **`minutos_del_dia(hora)`**.
- **`Franja`** (`desde_hora`, `hasta_hora`, `tope = 1`), con el comentario que
  dice lo que la ficha **F-269** anota: el `tope` son las piezas que la franja
  **ABRE** ese día, **no un máximo por franja**.
- **`Premio.franjas: list[Franja]`**, vacía por omisión = disponible a cualquier
  hora, como antes.
- **`ConfigHorario`** (`abre`, `cierra`, `fuera_de_horario = "consuelo"`), y
  `ConfigJuego` gana `separacion_min_entre_premios`, `espera_hora_seg` y
  `horario: ConfigHorario | None = None`. **Sin la llave `horario` no hay
  horario**: se juega todo el día, como antes de la Fase 4d.
- `desde_dict()` saca `horario` del crudo antes de construir `ConfigJuego`;
  `_premio()` convierte las franjas **antes** de verificar tipos, igual que hace
  con las fechas.
- **Funciones nuevas `_franjas()` y `_verificar_hora()`**, y **once mensajes de
  error nuevos en español**, todos nombrando la llave: hora mal escrita,
  `abre` posterior a `cierra`, `fuera_de_horario` inventado, `franjas` que no es
  lista, franja que no es objeto, llaves desconocidas, llaves faltantes,
  `desde_hora` posterior a `hasta_hora`, `tope` que no es entero ≥ 1, y los dos
  de `separacion_min_entre_premios` y `espera_hora_seg` negativos.

#### `ruleta/inventario.py` (+183 / −4)

- **Cabecera del módulo:** se añade el apartado **«El reparto por horas»** con la
  fórmula escrita, el ejemplo de las once aguas y la regla del arrastre.
- **Dos funciones puras nuevas.** **`instantes_de_liberacion(inicio, fin, cupo)`**
  reparte por **puntos medios** —la pieza *k* se abre en
  `inicio + (k − 0.5) × (fin − inicio) / cupo`— y **`_a_la_hora(dia, hora)`**.
- `ResumenPremio` gana **`liberadas`** y **`proxima`**.
- `Inventario.__init__` gana **`horario`** y **`separacion_min_entre_premios`**,
  y el atributo **`self._ultimo_premio`**.
- **Métodos nuevos:** `dentro_del_horario` (**el cierre es exclusivo**: a las
  23:00 en punto ya no se juega), `ventana_del_dia`, `franjas_del_dia`,
  `franja_activa`, `instantes_del_dia`, `liberadas`, `proxima_liberacion` y
  `espera_separacion`.
- **`motivo_no_disponible()`** gana cuatro motivos nuevos, en este orden:
  `"fuera de horario"`, `"franjas de hoy cerradas"` o
  `"su franja abre a las HH:MM"`, y `"todo lo de hoy ya salió"` o
  `"se libera a las HH:MM"`.
- **`sortear()`** devuelve `None` **sin tirar** si `espera_separacion(momento) > 0`.
- **`confirmar()`** guarda `self._ultimo_premio` **solo cuando el boleto lleva
  premio**, y lo **persiste** a disco; si el disco falla, `log.error` y sigue.
- **`estado.json` gana la llave `"ultimo_premio"`.** Un estado escrito por una
  versión anterior **no la trae**, y eso significa «sin separación pendiente»:
  **los estados viejos se siguen cargando enteros**. Un valor ilegible se ignora
  con un `log.warning`.
- **Un reloj que se va hacia atrás no bloquea el juego**: si el instante guardado
  queda en el futuro, `espera_separacion` devuelve `0.0`. Está escrito en el
  docstring y anclado por golden.

#### `ruleta/app.py` (+99 / −7)

- Constantes **`PERIODO_CONSULTA_HORA = 2.0`** y
  **`MARCA_TIMESYNC = Path("/run/systemd/timesync/synchronized")`**.
- **`hora_sincronizada_del_sistema()`**, la pieza D: mira primero el archivo que
  crea `systemd-timesyncd`; si no está, pregunta a `timedatectl`; **en una
  máquina sin ninguna de las dos contesta que sí**, para no quedarse esperando
  algo que nadie va a contestar.
- `Ruleta.__init__` gana **`hora_sincronizada`**, **inyectable**, que es lo que
  hace la pieza D comprobable sin hardware.
- **`esperar_hora_sincronizada()`**, llamado desde `arrancar()` **y solo desde
  ahí**: con el tope en 0 no espera nada; si se agota, deja el WARNING
  **«HORA SIN CONFIRMAR: el sistema no sincronizó la hora en %.0f s. Revisa la
  fecha del boleto de inventario antes de abrir: si está mal, NO reinicies,
  espera y pide otro inventario»**. Un comprobador que lanza excepción **no tira
  el arranque**: se registra con `log.exception` y se sigue sin confirmar.
- **Chequeo de horario en `jugar`:** fuera de horario con
  `"fuera_de_horario": "no_jugar"` **no se imprime nada** —ni papel ni folio— y
  solo avisa el LED. Para eso `_terminar_accion` gana **`contar_error=True`**:
  con `False`, el LED avisa pero **no se cuenta como avería**.
- El `log.info` del consuelo se abre en **tres** ramas: fuera de horario,
  separación pendiente («faltan %.1f min de separación desde el último premio») y
  el sorteo que cayó en el consuelo. El **warning** de «Sin premios disponibles»
  sigue reservado al caso de verdad.
- `imprimir_inventario` gana **`aviso_hora=False`**.

#### `ruleta/ticket.py` (+32 / −2)

- Constante **`AVISO_HORA = "HORA SIN CONFIRMAR: revisar fecha"`**, impresa **en
  negrita** en la cabecera del boleto de inventario cuando `aviso_hora=True`.
- **`renglones_de_liberacion(fila, ancho)`**, función **pura** —nunca lee el
  reloj— que arma el renglón del reparto de cada premio: `hoy N`,
  `liberadas N` y `sig HH:MM` o `sin más hoy`, unidos con **` · `** y sangrados
  con dos espacios.

#### `ruleta/__main__.py` (+9 / −2)

`abrir_inventario()` pasa además **`horario`** y
**`separacion_min_entre_premios`**, y su docstring deja escrito que es **el único
sitio de producción que construye un `Inventario`** (lo que la ficha F-265
corrigió en la fase anterior, ahora anclado por golden).

### 5.2 El reparto, calculado por el programa

**Recalculado por el escriba** sobre el `config.json` committeado, con
`Inventario.instantes_del_dia` y el día operativo del **jueves 24**. Coincide
**exactamente** con la tabla del archivo de hechos:

| Premio | A qué horas se abre cada pieza |
|---|---|
| `hielera` | 19:00 |
| `silla` | 13:00 · 19:00 |
| `bbq` | 13:00 · 19:00 |
| `tacos3` | 13:22:30 · 16:07:30 · 18:52:30 · 21:37:30 |
| `tacos2` | 13:22:30 · 16:07:30 · 18:52:30 · 21:37:30 |
| `cerveza` | 12:33 · 13:39 · 14:45 · 15:51 · 16:57 · 18:03 · 19:09 · 20:15 · 21:21 · 22:27 |
| `agua` | 12:30 · 13:30 · 14:30 · 15:30 · 16:30 · 17:30 · 18:30 · 19:30 · 20:30 · 21:30 · 22:30 |

**Simulación de un jueves completo** (30 corridas por renglón, con los premios,
las franjas y la separación reales):

| Si se juega… | Jugadas | Premios que salen |
|---|---|---|
| una cada **20 s** | 1 980 | **34 de 34** |
| una cada **60 s** | 660 | **34 de 34** |
| una cada **180 s** | 220 | **34 de 34** |
| una cada **600 s** | 66 | 31.7 de 34 |

**Esa es la ganancia de la fase:** salga la gente que salga, se entrega el cupo
del día casi completo. Con el modelo viejo, y **acertando** N, se entregaba el
84 %.

### 5.3 Goldens y mutaciones

**Suite de 232 a 268** pruebas en verde, **+36**. Medido por el escriba sobre el
árbol committeado, módulo por módulo: `tests.test_escpos` **85**,
`tests.test_inventario` **46**, `tests.test_instalacion` **42**,
`tests.test_app` **37**, `tests.test_config` **30** y `tests.test_ticket` **28**.
**85 + 46 + 42 + 37 + 30 + 28 = 268.**

Lo nuevo, por archivo:

- **`tests/test_inventario.py`:** clase nueva **`TestRepartoPorHoras`**, con
  dieciséis goldens. Los instantes se anclan **por igualdad de listas de
  `HH:MM`** (`test_instantes_de_liberacion_del_evento_real`,
  `test_instantes_de_liberacion_es_una_funcion_pura`); las liberadas y las
  disponibles, **a horas concretas**
  (`test_liberadas_y_disponibles_a_horas_concretas`,
  `test_lo_abierto_y_no_ganado_no_se_pierde_pero_no_adelanta`); las franjas
  (`test_franjas_de_la_silla_del_evento_real`,
  `test_el_tope_de_una_franja_se_respeta`,
  `test_la_hielera_solo_de_siete_a_once_de_la_noche`); el horario
  (`test_fuera_del_horario_no_hay_premios_y_el_sorteo_da_consuelo`); la
  separación y sus cuatro casos borde
  (`test_separacion_minima_entre_premios`,
  `test_la_separacion_solo_la_marca_un_boleto_impreso`,
  `test_sin_separacion_configurada_no_hay_regla`,
  `test_un_reloj_que_se_va_hacia_atras_no_bloquea_el_juego`); la persistencia
  (`test_el_ultimo_premio_sobrevive_al_reinicio`,
  `test_un_estado_viejo_sin_la_llave_carga_igual`); y el resumen
  (`test_el_resumen_trae_las_liberadas_y_la_proxima`,
  `test_un_premio_sin_tope_ni_franjas_no_se_reparte`).
- **`tests/test_config.py`:** `test_horario_del_evento_valido` e
  `…_invalido` (**diez** mensajes de horario), `test_franjas_validas` e
  `…_invalidas` (**nueve** de franjas), y **cinco goldens derivados del
  documento**: `test_config_json_lleva_el_bloque_de_juego_del_documento`,
  `test_el_5_2_dice_lo_mismo_que_el_5_1_en_los_tres_bloques_que_se_repiten`,
  `test_la_franja_de_ejemplo_del_5_2_B_es_la_de_la_hielera`,
  `test_las_probabilidades_del_2_las_calcula_el_programa` y
  `test_la_nota_historica_del_2_sigue_obedeciendo_su_regla_N_menos_33`.
- **`tests/test_app.py`:** los tres del horario
  (`test_fuera_de_horario_con_consuelo_imprime_consuelo`,
  `…_con_no_jugar_no_imprime_nada`, `test_dentro_del_horario_se_juega_normal`) y
  los cuatro de la hora (`test_arranque_espera_a_que_la_hora_se_sincronice`
  —el comprobador falso **no / no / sí**—,
  `test_arranque_avisa_en_el_boleto_si_la_hora_no_se_confirma`,
  `test_sin_espera_configurada_no_se_pregunta_la_hora` y
  `test_un_comprobador_de_hora_que_falla_no_tira_el_arranque`).
- **`tests/test_instalacion.py`:** `test_abrir_inventario_pasa_el_horario_y_la_separacion`
  y `test_el_config_json_de_produccion_llega_entero_al_motor`.
- **`tests/test_ticket.py`:** `test_inventario_del_config_real_trae_el_reparto_por_horas`,
  `test_inventario_avisa_cuando_la_hora_no_se_confirmo` y
  `test_un_premio_sin_reparto_no_lleva_renglon_de_liberacion`.

**Catorce mutaciones, las catorce en rojo**, corridas con el arnés
`scratchpad/mutar.py`, que copia el repositorio y corre la suite entera en cada
copia. **En las catorce cayó el test que la tabla del §5 del plan predecía.** Las
tres que demuestran que los goldens **derivan**: **M13** (la franja de la hielera
a `18:00` **en el documento**), **M14** (la tabla de la tómbola del §2 **en el
documento**) y **M12**, que lo demuestra por el otro lado (tocando **solo**
`config.json`, cae el golden del bloque `juego`).

### 5.4 Revisión, commit y fichas

- **Lentes:** **7** + **3** correcciones, con **2 sin converger** aplicadas **por
  orden del orquestador** (§9.5). **Escéptico: 2**, y las dos mordieron de
  verdad: un comentario y un mensaje de error de `ruleta/config.py` decían que el
  `tope` de una franja es «el máximo de esa franja ese día», **y eso es falso**
  con la lectura que el programa implementa (ficha **F-269**). Quedaron
  reescritos en el mismo commit.
- **Fichas: 266 → 272.** **F-267**, **F-268**, **F-269**, **F-270** (el peso 10
  lo propuso el asistente, no el usuario), **F-271** (un horario que cruce la
  medianoche no se puede escribir) y **F-272** (`hardware.py` permitido y no
  tocado). **F-260 y F-266 quedaron cerradas** en este mismo commit.
- **Commit `613f875`**, con los quince archivos del §7 del plan. Push verificado.

### 5.5 Deploy y verificación en vivo (12:42:46)

- `date` en la Pi: **«Wed 16 Sep 12:42:39 MST 2026»**, **`NTPSynchronized=yes`**.
- `git pull --ff-only` → **`613f875`**; **268 pruebas OK en la Pi**;
  `restart --no-block`; `active`, **`NRestarts=0`**, **PID 3560**.
- Journal:
  `Premios: hielera, silla, bbq, tacos3, tacos2, cerveza, agua`,
  `Inventario impreso (arranque). Folio actual 00005`, `Lista. Esperando jugadas.`
  **0 ERROR.**
- **Cuando todo va bien, la pieza D no deja ni una línea en el journal**: solo se
  registra el fallo. Es un hueco de observabilidad, no de conducta: ficha nueva
  **F-273**.
- Verificado en vivo por un agente distinto (`hora_pi` 12:45:11, `NTP yes`).

### 5.6 El reinicio del inventario de las 12:46

Para que el usuario probara **limpio**, un agente reinició el inventario a las
**12:46**, con estos números medidos antes: **folio 6**, entregados **agua 3,
bbq 1, cerveza 1**, más **un consuelo, el 00006, a las 12:46:12**, emitido
todavía por el proceso viejo.

`stop` → `reiniciar --si` → respaldos **`datos/estado_20260916_124629.json`** y
**`datos/boletos_20260916_124629.csv`**, «Folio en 00000» → `start` a las
**12:46:48** → `active`, **`NRestarts=0`**, `Inventario impreso (arranque). Folio
actual 00000`, **0 ERROR / 0 WARNING** en el proceso nuevo.

---

## 6. La prueba en vivo del usuario (~13:00)

Es **el criterio que de verdad cierra las tres fases**. Con `613f875`
desplegado, el inventario en **folio 00000** y el reloj del evento ya abierto, el
usuario jugó y dijo, literal:

> **«ya probé, salieron consuelos y una cerveza, funciona bien».**

**Cuadra con el reparto de la §5.2**, y esa es la parte que se puede comprobar
contra los números: si esas jugadas cayeron **antes de las 13:00**, las únicas
piezas abiertas eran **el agua (12:30)** y **la cerveza (12:33)**; **a partir de
las 13:00** se abren además **una silla y un set BBQ**. **La hora exacta de esas
jugadas NO se midió**, así que cuál de los dos casos fue no está determinado. En
los dos rige la **separación de 3 minutos** entre dos premios. Salió **una** cerveza y el resto consuelos, que es exactamente lo que el
modelo predice para el primer cuarto de hora de un día que empieza en cero.

**Lo que esta frase NO demuestra**, y se dice aquí para que nadie lo dé por
medido: nadie pegó el journal ni el `boletos.csv` de esas jugadas. **No hay
conteo de cuántas fueron, ni a qué hora exacta, ni el renglón del reparto en un
boleto de papel.** Lo que hay es la conducta observada por el usuario y la
coherencia con la tabla. Está en la §11.

---

## 7. Decisiones del ejecutor que el orquestador aceptó

Las tres son lecturas del ejecutor sobre puntos que el plan **no cerraba del
todo**. En los tres casos el ejecutor **no improvisó**: implementó lo escrito, lo
anotó en una ficha y lo dejó a decisión del orquestador. **El orquestador las
aceptó el 2026-09-16**, y la aceptación quedó anotada con fecha en cada ficha.

| Ficha | Qué se aceptó | Qué implica |
|---|---|---|
| **F-267** | Sin `juego.horario`, el reparto se hace sobre **el día operativo completo** (`hora_inicio_dia` + 24 h) | Cambia la conducta de `tope_diario` en instalaciones **sin** horario: un premio de una pieza al día no se puede ganar hasta las 18:00. **En el Asadero 33 no cambia nada**, porque su `config.json` sí trae horario |
| **F-269** | El tope de una franja **no se persiste por franja**: lo abierto y no ganado **se arrastra dentro del día** | A las 19:00, con la silla de la comida sin ganar, puede haber **dos sillas** disponibles. Lo que nunca se pasa es el `tope_diario`. La alternativa exigiría un contador por franja en `estado.json`, es decir, estado nuevo y persistido, en la fase que abre el evento el lunes |
| **F-268** (decisión **D4**) | La separación la marca **solo un boleto impreso con premio** | Un boleto **`incierto`** —la impresión se cortó a medio envío— no apunta el instante, así que la jugada siguiente puede dar otro premio de inmediato. Por cable USB es un caso raro y benigno: lo peor es que salgan dos premios pegados, no un premio de más |

**La F-268 sigue abierta por otra cosa:** hoy **ningún golden** ata ese
comportamiento —`test_la_separacion_solo_la_marca_un_boleto_impreso` ancla el
boleto revertido y el consuelo confirmado, pero **ninguna prueba llama a
`marcar_incierto()` junto a `espera_separacion()`**—, así que la conducta
aceptada se puede cambiar **sin que la suite se ponga en rojo**. Falta ese
golden.

---

## 8. Cómo quedan los premios después de las tres fases

Resumen operativo, todo derivado del `config.json` committeado en `613f875`:

- **Siete premios reales**, 167 piezas, **34 de cupo diario sumado** (33 sin la
  hielera).
- **Horario del evento 12:00–23:00**, cierre **exclusivo**. Fuera de él **no hay
  ningún premio disponible** y la jugada sale de **consuelo**.
- **Franjas:** hielera solo de 19:00 a 23:00; silla y set BBQ, una pieza de 13:00
  a 16:00 y otra de 19:00 a 22:00.
- **Tacos, cerveza y agua** repartidos parejo por **puntos medios** del cupo
  diario a lo largo de las once horas (§5.2).
- **Consuelo con peso 10**, compitiendo contra lo que esté abierto en ese
  momento.
- **Separación mínima de 3 minutos** entre dos boletos con premio, **persistida**
  en `estado.json`.
- **Espera de hora al arrancar de hasta 120 s**, preguntando cada 2 s, con la
  línea **`HORA SIN CONFIRMAR: revisar fecha`** en el boleto de inventario si no
  lo consigue.
- **Las fechas `desde`/`hasta` siguen SIN cargar** (**F-259**): hasta que se
  carguen, los premios están disponibles **todos los días**, no solo del 21 al
  25, y la hielera entra todos los días mientras le queden sus dos piezas.

---

## 9. Desviaciones

1. **El plan y el código fueron en un solo commit, en las tres fases.** El §3 del
   protocolo de `CLAUDE.md` separa el commit del plan del commit del cambio.
   Aquí no: cada plan viaja **dentro** del commit de su fase. Motivo escrito en
   los tres planes: el usuario estaba **probando en vivo el 2026-09-16** y el
   evento abre el **lunes 21**. **Los eslabones de revisión no se saltaron**:
   lentes, correctivo, escéptico, commit compuertado, deploy y verificación van
   después, como siempre.
2. **`reiniciar` respalda `estado.json` y `boletos.csv`, y NO `ruleta.log`.**
   Se dice aquí porque las dos veces que se corrió en esta sesión el resumen
   podría leerse como «se respaldó todo». **No.** Además `boletos.csv` **se
   mueve** al respaldo y se vuelve a crear con el primer boleto: entre el
   reinicio y la primera jugada, ese archivo **no existe**.
3. **El folio no estaba en cero al desplegar la 4c ni la 4d.** Los planes de
   ambas fases prohibían reiniciar el inventario y jugar durante el deploy, y no
   se hizo; pero **el usuario sí jugaba entre fases**, así que el folio iba en
   **5** al desplegar la 4c y en **6** justo antes del reinicio de las 12:46.
   **Los boletos de esas pruebas descontaron stock real.**
4. **El reinicio de las 12:46 lo hizo un agente, fuera del plan de la 4d**, que
   decía «NO `python3 -m ruleta reiniciar`». Fue **decisión del orquestador**,
   para que el usuario probara el reparto por horas desde cero y no contra un
   inventario a medias. Está medido en la §5.6, con sus dos respaldos.
5. **Correcciones de los lentes aplicadas sin converger.** Tres en la Fase 4c y
   dos en la 4d se aplicaron **por orden del orquestador** en vez de cerrar una
   ronda más. El bucle correctivo está acotado a propósito (§5 del protocolo); lo
   residual va a ficha, no a una tercera ronda.
6. **Este acta cubre TRES fases en un solo documento**, en vez de una por fase
   como en las Fases 1, 2, 3 y 4a. Decisión del orquestador, por la misma razón
   que la desviación 1: las tres fases se hicieron el mismo día, sobre el mismo
   archivo de hechos y con el mismo usuario probando en vivo. Ficha **F-274**.
7. **Todo lo medido en la Pi se midió en la red de laboratorio**, no en el
   asadero: el Wi-Fi de la casa del usuario (`SL-Durazo`, `192.168.1.212`). Nada
   del reparto por horas depende de la red —solo la **hora**, y esa sí—, pero se
   dice para que nadie lo dé por probado «en el sitio». El perfil Wi-Fi del
   asadero **todavía no está en la Pi**.

---

## 10. Lo que estas fases dejan abierto (para el lunes 21)

Los seis pendientes del archivo de hechos, en su orden:

| # | Pendiente | Quién | Ficha |
|---|---|---|---|
| 1 | **Cargar `desde`/`hasta`** (hielera `2026-09-24`…`25`; el resto `2026-09-21`…`25`) y **ajustar el golden** que hoy ancla «todavía no traen fechas» | fase de configuración | **F-259** |
| 2 | **Reiniciar el inventario antes de abrir el lunes**, con el servicio parado | usuario o agente | **F-262**, **F-243** |
| 3 | **Perfil Wi-Fi del asadero en la Pi.** El perfil viejo desapareció y **la contraseña la teclea el usuario**: ningún agente lo hace | **requiere al usuario** | — |
| 4 | **Soldar o asegurar** el pulsador HABILITAR | usuario | **F-239** |
| 5 | **Señal perceptible sin LED** cuando una jugada se rechaza | **decisión del usuario** | **F-256**, **F-240** |
| 6 | **`CLAUDE.md`, «Contexto del producto»**: el inventario ya no está en folio 16 y falta el modelo de reparto por horas | escriba / orquestador | **hecho en este cierre** |

Además, lo que queda de las propias fases:

- **El número 10 del consuelo lo propuso el asistente, no el usuario**
  (**F-270**, heredera de **F-263**).
- **Falta el golden de la F-268** (el boleto `incierto` y la separación).
- **Un horario que cruce la medianoche no se puede escribir**, y nadie avisa de
  que `hora_inicio_dia` y `horario` pueden pelearse (**F-271**).
- **La pieza D no deja rastro en el journal cuando todo va bien** (**F-273**).
- **La pregunta 7 del §4** del documento del evento (factor de holgura) se queda
  sin responder; con el reparto por horas casi no aplica, y el documento lo dice.

---

## 11. Dudas que este acta deja abiertas

1. **El archivo de hechos y el diff real difieren en un porcentaje, y los dos
   tienen razón.** El archivo de hechos cita la línea del boleto de inventario
   como «`SIGUE PARTICIPANDO (consuelo)  peso 217 -> 86.5%`», y el golden
   committeado ancla «`… peso 217 -> 96.9%`». **No se contradicen: son dos
   vectores distintos.** El **96.9 %** es el de
   `test_inventario_con_peso_de_consuelo`, que usa los **tres premios de prueba**
   del test (pesos 5 + 1 + 1 = 7, más 217 = 224 → 217/224). El **86.5 %** es el
   del `config.json` **real** (34 + 217 = 251 → 217/251), y ahí el golden
   `test_inventario_del_config_real_cabe_entero` **no ancla el número**: lo
   deriva del propio resumen. Se anota porque un lector futuro podría creer que
   uno de los dos documentos miente.
2. **Nadie midió por qué `reiniciar` dijo «Premios entregados registrados: 0»
   con el folio en 16.** La salida literal está en la §3.4. La explicación
   probable —los contadores en disco iban a nombre de `test4`…`test7`, que ya no
   son premios del `config.json` recién cargado— **es una inferencia del escriba,
   no una medición**. Lo que sí está medido es que el respaldo se escribió y que
   el folio quedó en 00000.
3. **La prueba en vivo del usuario no tiene evidencia pegada.** Es la duda más
   grande de este cierre: de las tres pruebas en vivo, la que **cierra** las tres
   fases es la única de la que **no hay journal, ni `boletos.csv`, ni foto del
   papel**. Lo que hay es la frase del usuario y su coherencia con el reparto
   (§6).
4. **El renglón del reparto por horas no se ha visto en papel.** Está anclado por
   golden a 48 columnas y se imprimió en el boleto de arranque de las 12:46,
   pero **nadie pegó ese boleto ni lo leyó**. Que quepa está medido; que se
   entienda, no.
5. **La pieza D nunca se ha visto morder en la Pi.** El servicio arrancó
   **cuatro** veces el 2026-09-16 —00:11:02, 10:47:25, 12:42:46 y 12:46:48—,
   pero la pieza D **no existía antes de `613f875`**, así que solo corrió en las
   **dos últimas**: en la de las 12:42:46, con `NTPSynchronized` medido en
   `yes`, y en la de las 12:46:48, con **0 WARNING** en el proceso nuevo. Las
   dos veces la espera **devolvió True de inmediato** y el aviso
   `HORA SIN CONFIRMAR` **no se ha impreso nunca en papel**. Lo que está probado son los goldens con el
   comprobador inyectado. **El caso real —Pi encendida sin hora— sigue sin
   provocarse.**
6. **La separación de 3 minutos no se ha visto morder en la Pi.** Los goldens la
   fijan; el journal de la prueba del usuario, que es donde habría salido la
   línea «consuelo obligado, faltan … min», no se leyó.
7. **Las simulaciones de la §5.2 se corrieron en la PC**, con el reloj inyectado,
   no en la Pi. Lo que dicen es cómo se comporta el modelo, no cómo se comporta
   el hardware un jueves lleno.
8. **Las fechas siguen sin cargar, y eso cambia los números.** Todo lo que este
   acta dice sobre cupos y probabilidades está calculado **sin `desde`/`hasta`**,
   así que la hielera entra todos los días. Cuando se carguen (**F-259**), la
   tabla del reparto no cambia, pero **los días en que cada premio existe, sí**.

---

## Regla final

**Si el código real, el `README.md`, el `config.json` o la propia Raspberry Pi
contradicen este acta o los planes de las Fases 4b, 4c o 4d, el ejecutor se
detiene y pregunta; no improvisa.**

Y la que dejaron escritas estas tres fases, porque las tres nacieron de que el
usuario probara en papel lo que un documento prometía: **el documento del evento
manda sobre `config.json`** (§6, paso 1), y **ningún criterio se da por cumplido
«porque tiene sentido»**. Se mide, se pega la salida, y si no se pudo medir, se
escribe que no se midió.
