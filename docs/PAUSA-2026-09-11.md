# Pausa segura · 2026-09-11 (tarde)

> ## ESTADO: **RETOMADA**
>
> La pausa se levantó y el eslabón 7 (`f2:ejecutor:plan`), que era donde se había
> detenido la cadena, **ya se ejecutó**: el plan de la Fase 2 existe y está
> escrito en **`docs/planes/fase-2-impresora.md`** (sin commitear todavía).
>
> Ese plan incorpora los hechos del §4 de este archivo (Zjiang ZJ-80250,
> cortador y zumbador, `DLE EOT 1`, binarios x86 inservibles) **y los hechos
> nuevos del día siguiente**: la Pi y la PC están en otra ubicación con otro
> Wi-Fi, la Pi solo conoce la red del asadero, la PC no tiene Ethernet ni WSL, y
> la impresora **ya está conectada por USB y con corriente**. De ahí sale el
> **Paso 0 «Red de laboratorio»** del plan nuevo: el punto de acceso móvil de
> Windows con el mismo nombre y la misma contraseña del Wi-Fi del asadero.
>
> **Lo que sigue pendiente de esta pausa:** los eslabones 8 a 12 (lentes de la
> Fase 2, correctivo, escéptico, commit y verificador de push), y la ejecución
> del plan con la impresora delante. El resto de este archivo se deja **tal como
> estaba**, porque describe cómo se llegó hasta aquí; lo único añadido es este
> aviso.

Escrito por el orquestador (Fable) a petición del usuario antes de moverse de lugar.
Este archivo describe EXACTAMENTE dónde se detuvo el trabajo y cómo retomarlo. Es
la fuente durable; la memoria de sesión tiene un resumen equivalente.

## 1. Estado al pausar

| Qué | Estado |
|---|---|
| Repo local `C:/Users/seduv/Desktop/Ruleta Asadero` | árbol limpio (verificado por agente de solo lectura tras la detención) |
| HEAD = origin/main | `601c4c2b64fc93e2a76b539f2b79fee9e1f91843` (4 commits en `main`, publicados en https://github.com/seduva94/Ruelta-Aleatoria-Pi5.git) |
| Raspberry Pi 5 (`ruleta`, usuario `asadero`) | Fase 1 completada y verificada: OS Trixie Lite 64-bit, `~/ruleta` clonado en `2052e47`, paquetes instalados, servicio `ruleta` **enabled e inactive** (no arrancado). Puede apagarse; nada corre en ella |
| Impresora AOMU My-A1 (= Zjiang ZJ-80250) | sin probar; el usuario no trajo el cable de corriente. Mañana: conectar por **USB** (decisión del usuario) |
| Cadena en curso | **detenida** con TaskStop: workflow `ruleta-cierre-fase1-y-plan-fase2`, run `wf_b25a2657-0cd`, task `wacpbbgs5` |

Archivos que este pausado deja SIN commitear a propósito (se commitean al retomar):
- `docs/PAUSA-2026-09-11.md` (este archivo)
- `docs/actas/2026-09-11-hechos-medidos.md` (copia del archivo de hechos medidos del scratchpad de la sesión; incluye los hechos del driver del fabricante que el acta de Fase 1 aún no tiene)

## 2. Dónde se quedó el agente, exactamente

La cadena tenía 12 eslabones. Se completaron los 6 de Fase 1 y se detuvo en el 7.º:

1. `f1:ejecutor:cierre` ✅ (plan F1 con bitácora marcada y §0-bis desviaciones; acta `docs/actas/2026-09-11-fase-1.md`; fichas)
2. `f1:lente:conducta:r1` / `f1:lente:fidelidad:r1` ✅ → `f1:correctivo:r1` ✅ (10 correcciones, 16 fichas)
3. `f1:lente:conducta:r2` / `f1:lente:fidelidad:r2` ✅ (ambos listo=true) → `f1:correctivo:r2` ✅ (0 correcciones, 7 fichas)
4. `f1:esceptico` ✅ (2 correcciones en §7 punto 6 del acta) → `f1:correctivo:esceptico` ✅ (2 aplicadas, 11 fichas)
5. `f1:commit` ✅ commit `601c4c2` con 5 rutas: plan F1, acta F1, fichas, y **bit de ejecución 100755** en `instalar.sh` y `herramientas/emparejar.sh`. Push hecho
6. `f1:verificar-push` ✅ push_correcto=true, 7/7 comprobaciones
7. `f2:ejecutor:plan` ⏹ **DETENIDO antes de escribir nada** (`docs/planes/fase-2-impresora.md` NO existe; `docs/fichas.md` sin cambios)
8-12. F2 lentes, correctivo, escéptico, commit, verificador: **no iniciados**

## 3. Qué falta

### 3.1 Plan de la Fase 2 (impresora por USB, respaldo Bluetooth)
Escribir `docs/planes/fase-2-impresora.md` y pasarlo por la cadena completa. El brief
ya está en el script del workflow (ver §5). Debe incorporar los hechos del driver del
fabricante (§4 de este archivo y `docs/actas/2026-09-11-hechos-medidos.md`).

### 3.2 Ejecución de la Fase 2 (mañana, con la impresora)
Requiere del usuario: impresora + cable de corriente + cable USB A-B + rollo de 80 mm;
Pi encendida y en el mismo Wi-Fi que la PC. Luego, por agentes vía SSH (alias `ruleta`,
cliente `C:/Windows/System32/OpenSSH/ssh.exe`, PATH remoto sin `/usr/sbin`):
1. Detección: `lsusb`, `dmesg | tail`, `ls -l /dev/usb/lp*`, `udevadm info`. Se espera
   clase PRINTER → driver `usblp` → `/dev/usb/lp0` (1284 ID `MFG:Zijiang;...;MDL:ZJ-80250`).
2. Permisos: `/dev/usb/lp0` es `root:lp 0660` y `asadero` NO está en `lp` → regla udev
   por VID:PID reales (medir primero) o `usermod -aG lp asadero`.
3. En la Pi editar `config.json`: `"tipo": "archivo"`, `"ruta": "/dev/usb/lp0"`.
4. `python3 -m ruleta diagnostico` y `python3 -m ruleta probar-impresora` (servicio detenido).
   El usuario lee el papel: tabla de acentos (0/2/16/19 → `codepage`/`codepage_n`),
   regla de ancho (42 vs 48, DIP 5), corte (`auto` vs `parcial`), logo, zumbador.
5. Sincronizar el `config.json` final al repo por la cadena; en la Pi `git checkout -- config.json && git pull`.
6. Sub-fase 2b (código, con goldens, por la cadena): udev en `instalar.sh`; consulta
   de papel en `ImpresoraArchivo` (DLE EOT por `/dev/usb/lp0`) o documentar que no hay;
   `diagnostico` prueba el tipo `archivo` y muestra versiones con `importlib.metadata`;
   README con USB de primera clase y zona horaria real.
7. Arrancar el servicio y verificar en vivo (`journalctl -u ruleta`: "Lista. Esperando
   jugadas." e "Inventario impreso (arranque)"; el inventario sale por la impresora).
8. Acta de Fase 2 desde hechos medidos.

### 3.3 Decisiones pendientes del usuario (no bloquean el plan, sí la ejecución de algunas fichas)
- **Privacidad (aviso del agente de commit):** el acta de Fase 1, ya publicada en el repo
  PÚBLICO, contiene la IP local 192.168.50.178, la IPv6 de enlace local (lleva la MAC de
  la Pi), el hostname, el usuario `asadero` y la regla `asadero ALL=(ALL) NOPASSWD: ALL`.
  No son secretos (la Pi irá sin red), pero el usuario debe decidir si lo deja o si se
  reescribe (implicaría reescribir historial público).
- **README contradice decisiones cerradas** (fichas F-001, F-002, F-003, F-052, F-053):
  copia por `scp` vs `git clone`, usuario genérico, `set-ntp false`, `America/Mexico_City`
  vs `America/Hermosillo`, grupo `lp`. Falta visto bueno para reescribir el README de una
  pasada (va dentro de la sub-fase 2b).
- **Goldens no medidos** en Fase 1: `systemctl show -p SubState --value ruleta` (dead),
  `timedatectl show -p NTP --value` y `-p LocalRTC --value`. Se pueden medir mañana con
  la Pi encendida antes de empezar la Fase 2.
- **Duda abierta:** el plan F1 afirmaba huellas previas de `ruleta.local` en `known_hosts`
  que harían fallar la primera conexión; no falló. Sin causa registrada.

## 4. Hechos nuevos de hoy que el plan de Fase 2 debe usar
- La impresora es una **Zjiang ZJ-80250** (PPD del driver Linux del fabricante:
  `MFG:Zijiang;CMD:Zijiang;MDL:ZJ-80250;CLS:PRINTER`). Clase USB PRINTER → `usblp`.
- Tiene **cortador y zumbador** (opciones Cutting y Beeper del PPD). El filtro del
  fabricante usa `DLE EOT 1` (estado en tiempo real).
- Los binarios del driver son x86: **no sirven en la Pi**; seguimos con ESC/POS propio.
- Sin VID:PID en el paquete: se mide en la Pi con `lsusb`.
- Producción **sin red** (decisión del usuario): batería RTC necesaria; SSH solo para
  instalar y probar.

## 5. Cómo retomar (orden exacto)
1. Leer: este archivo, `CLAUDE.md`, `docs/actas/2026-09-11-fase-1.md`,
   `docs/planes/fase-1-preparar-pi.md` (§0-bis y §9) y `docs/fichas.md`.
2. Verificador de solo lectura (Sonnet): `git status --porcelain` muestra SOLO los dos
   archivos del §1; `HEAD` = `origin/main` = `601c4c2`; si la Pi está encendida,
   `ssh ruleta hostname` → `ruleta` (si está apagada, no es bloqueo para el plan).
3. Relanzar la cadena. Opción A (recomendada): `Workflow` con
   `scriptPath = C:/Users/seduv/.claude/projects/C--Users-seduv-Desktop-Ruleta-Asadero/5bf7e7ff-82b1-41cd-80f4-42ffcfb9b5c2/workflows/scripts/ruleta-cierre-fase1-y-plan-fase2-wf_b25a2657-0cd.js`
   y `resumeFromRunId = wf_b25a2657-0cd`: los 11 agentes de Fase 1 regresan de caché y
   arranca en `f2:ejecutor:plan`. **Antes** de relanzar, editar en ese script
   `permitidos2` para incluir `docs/PAUSA-2026-09-11.md` y
   `docs/actas/2026-09-11-hechos-medidos.md` (si no, el gate del agente de commit
   de Fase 2 detendrá el commit por rutas fuera del conjunto). Ajustar también el
   verificador F2 (`conteoEsperado` sigue siendo 5).
   Opción B: nuevo workflow solo con la cadena de Fase 2 (mismo brief del ejecutor F2,
   copiado del script), con `baseEsperada = 601c4c2...` y `conteoEsperado = 5`.
4. Cuando llegue la impresora: seguir `docs/planes/fase-2-impresora.md` ya commiteado.

Regla del protocolo que aplica: si el código real, el README o la Pi contradicen el
plan, el ejecutor se detiene y pregunta; no improvisa.
